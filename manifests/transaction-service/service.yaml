apiVersion: v1
kind: Service
metadata:
  name: transaction-service
  labels:
    app: transaction-service
spec:
  type: NodePort
  selector:
    app: transaction-service
  ports:
    - port: 8082
      targetPort: 8082
